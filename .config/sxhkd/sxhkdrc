### Basic binds ###
#File manager


### Tools ###
# Rofi{{{
super + r
	j4-dmenu-desktop --term=alacritty
super + shift + r
	rofi -show drun

# Show windows
alt + Tab
	rofi -show window
# Show commands
super + F2
	rofi -show run
# Show ssh
super + F3
	rofi -show ssh
# Clipboard history
super + c
	rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'
# Show emojis
super + slash
	rofi -show emoji
# }}}

# Special keys{{{
# Terminal
super + Return
	alacritty
	# alacritty msg create-window || alacritty

# File manager
super + shift + Return
	nemo

# Screenshot
Print
	flameshot gui

# Escanear
super + Print
	simple-scan

# Htop
control + shift + Escape
	alacritty -e htop

# Web browser
super + alt + q
	google-chrome-unstable
super + e
	firefox-developer-edition
super + w
	qutebrowser

# Bookmarks
# super + b
# 	alacritty --title buku -e /home/tony/.scripts/bfind
# super + shift + b
# 	/home/tony/.scripts/bukuadd

# Kill
super + shift + x
	xkill

# Volume control
super + v
	pavucontrol
super + control + shift + v
	alacritty -e pulsemixer

# }}}

# Media keys{{{
# Brillo
XF86MonBrightnessDown
	/home/tony/.scripts/change-brightness down
XF86MonBrightnessUp
	/home/tony/.scripts/change-brightness up


# Rotate screen
XF86Display
	alacritty -e /home/tony/.scripts/rotate-screen

# Mail
XF86Tools
	thunderbird

# Bluetooth
XF86Bluetooth
	blueberry

# Rofi beats
XF86Favorites
	alacritty --title rofi-beats -e /home/tony/.scripts/rofi-beats
#}}}

### Apps ###
## App packs{{{
# (Art) Asep | Blen | Ink | Kri | Tcty
super + alt + a ; {a, b, i, k, t}
	{aseprite, blender, inkscape, krita, tenacity}

# (IDEs) Ard | Cod | Gen | Net | Pyc | VSC
super + alt + i ; {a, c, g, n, p, v}
	{arduino, codeblocks, geany, netbeans, pycharm, vscodium}

# (Readers) Cali | Foli | Tach | Oku
super + alt + r ; {c, f, t, o}
	{calibre, foliate, tachidesk-server-electron, okular}

# (Misc) Calibre | Cheese
super + alt + m ; {c,p}
	{calibre,cheese}
# }}}

## Apps
# Virtualbox
super + alt + b
	virtualbox

# Cmus
super + alt + c
	alacritty --title cmus -e cmus

# Discord
super + alt + d
	discord

# FreeCAD
super + alt + f
	freecad

# Godot
super + alt + g
	godot

# Slack
super + alt + k
	slack

# Libreoffice
super + alt + l
	libreoffice

# Simplenote
super + alt + n
	alacritty --title sncli -e sncli -c /home/tony/.config/sncli/snclirc

# OBS
super + alt + o
	obs-studio

# Image viewer
super + alt + p
	qimgv

# Steam
super + alt + s
	steam

# Trello
super + alt + t
	trello

# VLC
super + alt + v
	vlc

# Spotify
super + alt + y
	alacritty --title ncspot -e ncspot

# Whatsapp
super + alt + z
	whatsapp-nativefier

# super + alt       OCUPADOS            LIBRES
#               abcdfghiklmnopqstvyz    ehjuwx

# super             OCUPADOS                        LIBRES
#               bcdefhijklmpqrtvwxz.,-= Fk F2 F3       agnosy
